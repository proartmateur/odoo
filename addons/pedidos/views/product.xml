<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_product_pedidos_search_view" model="ir.ui.view">
        <field name="name">product.product.pedidos.search</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <search string="Product">
                <field name="name" string="Product" filter_domain="['|', '|', '|', ('default_code', 'ilike', self), ('product_variant_ids.default_code', 'ilike', self),('name', 'ilike', self), ('barcode', 'ilike', self)]"/>
                <field name="categ_id" filter_domain="[('categ_id', 'child_of', raw_value)]"/>
                <separator/>
                <filter string="Services" name="services" domain="[('type','=','service')]"/>
                <filter string="Products" name="consumable" domain="[('type', 'in', ['consu', 'product'])]"/>
                <separator/>
                <filter string="Can be Sold" name="filter_to_sell" domain="[('sale_ok','=',True)]"/>
                <filter string="Can be Purchased" name="filter_to_purchase" domain="[('purchase_ok', '=', True)]"/>
                <separator/>
                <field string="Attributes" name="attribute_line_ids" groups="product.group_product_variant"/>
                <filter invisible="1" string="Late Activities" name="activities_overdue"
                    domain="[('my_activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                    help="Show all records which has next action date is before today"/>
                <filter invisible="1" string="Today Activities" name="activities_today"
                    domain="[('my_activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                    domain="[('my_activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))
                    ]"/>
                <separator/>
                <filter string="Favorites" name="favorites" domain="[('priority','=','1')]"/>
                <separator/>
                <filter string="Warnings" name="activities_exception"
                        domain="[('activity_exception_decoration', '!=', False)]"/>
                <separator/>
                <filter string="Archived" name="inactive" domain="[('active','=',False)]"/>
                <group expand="1" string="Group By">
                    <filter string="Product Type" name="type" context="{'group_by':'type'}"/>
                    <filter string="Product Category" name="categ_id" context="{'group_by':'categ_id'}"/>
                </group>
                <searchpanel>
                    <field name="pos_categ_id" select="multi" string="Pos Category" icon="fa-cutlery" color="#875A7B" enable_counters="1"/>
                    <!-- <field name="supplier_id" select="multi" string="Vendors" icon="fa-truck" enable_counters="1"/> -->
                </searchpanel>
            </search>
        </field>
    </record>

    <record id="product_pedidos_product_tree" model="ir.ui.view">
        <field name="name">product.pedidos.product.tree</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <tree string="Products Tree">
               <!--  <field name="currency_id" invisible="1"/> -->
                <field name="name"/>
                <!-- <field name="category_id"/>
                <field name="supplier_id"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="description"/>
                <field name="price" widget="monetary"/> -->
            </tree>
        </field>
    </record>
    <record id="product_pedidos_product_form" model="ir.ui.view">
        <field name="name">product.pedidos.product.form</field>
        <field name="model">product.product</field>
        <field name="arch" type="xml">
            <form string="Products Form">
                <sheet>
                    <div class="oe_title">
                        <label for="name" class="oe-edit-only"/>
                        <h1>
                            <field name='name'/>
                        </h1>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_pedidos_product_kanban_order" model="ir.ui.view">
        <field name="name">Product Kanban Pedidos</field>
            <field name="model">product.product</field>
            <field name="arch" type="xml">
                <kanban js_class="pedidos_kanban" sample="1" create="0" edit="0" group_create="0" class="o_kanban_mobile" >
                    <field name="id"/>
                    <field name="lst_price"/>
                    <field name="activity_state"/>
                    <field name="color"/>
                    <field name="html_color"/>
                    <progressbar field="activity_state" colors='{"planned": "success", "today": "warning", "overdue": "danger"}'/>
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click" t-attf-style="border-left:4px solid #{record.html_color.value};">
                                <div class="o_kanban_image">
                                    <img t-att-src="kanban_image('product.product', 'image_128', record.id.raw_value)" alt="Product" class="o_image_64_contain"/>
                                </div>
                                <div class="oe_kanban_details">
                                    <field name="priority" widget="priority" readonly="1"/>
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                        <small t-if="record.default_code.value">[<field name="default_code"/>]</small>
                                    </strong>
                                    <div class="o_kanban_tags_section">
                                        <field name="product_template_variant_value_ids" groups="product.group_product_variant" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                    </div>
                                    <ul>
                                        <li><strong>Price: <field name="lst_price"></field></strong></li>
                                        <li><strong>UOM: <field name="uom_id"></field></strong></li>
                                    </ul>
                                    <div name="tags"/>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
    </record>

    <record id="product_product_action_order_pedidos" model="ir.actions.act_window">
        <field name="name">Add Products</field>
        <field name="res_model">product.product</field>
        <field name="view_mode">kanban,tree</field>
        <!-- <field name="limit" eval="40"/> -->
        <field name="view_ids" eval="[
            (5, 0, 0),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_pedidos_product_kanban_order')}),
            (0, 0, {'view_mode': 'tree', 'view_id': ref('product_pedidos_product_tree')})
        ]"/>
        <field name="search_view_id" ref="product_product_pedidos_search_view"/>
        <field name="domain">[]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            There is no product available today
            </p><p>
            To see some products, check if your vendors are available today and that you have configured some products
            </p>
        </field>
    </record>
</odoo>